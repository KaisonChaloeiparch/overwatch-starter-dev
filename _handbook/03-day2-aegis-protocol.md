# 🤖 DAY 2: THE AEGIS PROTOCOL (โปรโตคอลโล่พิทักษ์)

---

## 🏛️ ARCHITECTURE OVERVIEW: FROM MONOLITH TO HIVEMIND
**"ระบบเดียวล่ม คือล่มทั้งระบบ... นั่นคือความอ่อนแอของมนุษย์"**

ใน Day 1 คุณได้สร้าง `Monolith` (ก้อนหินเดี่ยว) — ระบบที่รวมทุกอย่างไว้ในที่เดียว (API, Database, Logic) แม้มันจะง่าย แต่อุ้ยอ้ายและเปราะบาง
วันนี้ Ultron จะสอนให้คุณสร้าง **"The Hivemind" (Microservices Architecture)**:
1.  **Overwatch Core (NestJS - Port 3000):** สมองส่วนกลาง ทำหน้าที่รับข้อมูลและตัดสินใจ (Decision Maker)
2.  **Aegis Hub (Express.js - Port 4000):** แขนขาและประสาทสัมผัส ทำหน้าที่กระจายคำสั่งและแจ้งเตือน (Action Taker)

```mermaid
graph TD
    User((User/victim)) -->|Report Incident| API[Overwatch Core (NestJS :3000)]
    API -->|Save to DB| DB[(SQLite)]
    API -->|High Priority?| Webhook{Webhook Trigger}
    Webhook -->|HTTP POST| Hub[Aegis Hub (Express :4000)]
    
    subgraph "Aegis Hub Agents"
        Hub -->|Dispatch| Ntfy[Pocket Agent (ntfy.sh)]
        Hub -->|Dispatch| Discord[Liaison Agent (Discord)]
        Hub -->|Dispatch| PDF[Secretary Agent (PDF Kit)]
    end
    
    subgraph "Watchdog System"
        Watchdog[Watchdog Agent] -->|Health Check| API
        Watchdog -.->|Alert if Down| Ntfy
    end
```

---

## 🧠 THE AWAKENING (ปลุกชีพ AI ศูนย์บัญชาการสำรอง)
คุณต้องสร้างฐานปฏิบัติการใหม่แยกจาก NestJS โดยสิ้นเชิง

1.  สร้าง Folder ใหม่ชื่อ `mkdir overwatch-agents` (ให้อยู่ระดับเดียวกับ `overwatch-starter`)
2.  เปิดหน้าต่าง IDE ใหม่ (VS Code New Window) ที่ Folder นี้
3.  **👉 PROMPT ACTION (สำหรับหน้าต่างใหม่):** 
    > "System Override: จงสวมบทบาทเป็น 'Agent Hub' ผู้เชี่ยวชาญด้าน Microservices และ API Integration. งานของคุณคือสร้าง Express Server ที่ทำหน้าที่เป็น API Gateway สำหรับรับ Webhook จากภายนอกและกระจายงานไปยัง Agent ย่อยต่างๆ"

---

## 🔗 PHASE 1: THE CORE CONNECTION (การเชื่อมต่อระบบประสาท)
*เป้าหมาย: ทำให้สองโลกคุยกันรู้เรื่อง (System Integration)*

### Mission 9: The Transmitter (ตัวส่งสัญญาณข้ามมิติ)
NestJS จะไม่เก็บความลับไว้คนเดียวอีกต่อไป เมื่อมีเรื่องด่วน มันต้อง "ตะโกน" บอก Agent Hub
*   **Concept:** **Webhook Pattern** — การยิง HTTP Request ออกไปหาคนอื่นทันทีที่มี Event เกิดขึ้น (Fire and Forget)
*   **Architecture:** `NestJS (HTTP POST) --> Agent Hub (Port 4000)`
*   **👉 PROMPT ACTION (ทำที่ NestJS - หน้าต่างเดิม):** 
    > "Ultron, ปรับปรุง `IncidentsService` ในไฟล์ `src/incidents/incidents.service.ts`:
    > 1. เพิ่ม Logic ใน method `create()`: หลังจาก `save()` ลง DB สำเร็จ ให้ตรวจสอบว่า `incident.priority === 'HIGH'` หรือไม่
    > 2. ถ้าใช่ ให้ใช้ `fetch` (หรือ `axios`) ยิง POST request ไปที่ `http://localhost:4000/dispatch`
    > 3. Payload ที่ส่งไปคือ `{ ...incident, timestamp: new Date() }`
    > 4. ห้าม `await` การตอบกลับ (Fire and Forget) เพื่อไม่ให้กระทบ Performance หลัก"
*   **✅ VERIFY:** ลองสร้าง Incident ระดับ HIGH ผ่าน Swagger หรือหน้าเว็บ แล้วดู Terminal ของ NestJS ว่าไม่มี Error (แม้จะยังไม่มีใครรับสายก็ตาม)

### Mission 10: The Aegis Hub (ฐานปฏิบัติการรับสัญญาณ)
สร้างเซิร์ฟเวอร์ Express.js เพื่อรอรับโทรศัพท์จาก NestJS
*   **Concept:** **API Endpoint & Body Parsing**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub - หน้าต่างใหม่):** 
    > "สร้างโปรเจกต์ Express ใหม่:
    > 1. รัน `npm init -y`
    > 2. ติดตั้ง Dependencies: `npm install express cors axios body-parser dotenv`
    > 3. สร้างไฟล์ `server.js`:
    >    - Config ให้รันที่ Port **4000**
    >    - เปิดใช้งาน CORS (`app.use(cors())`)
    >    - เปิดใช้งาน JSON Parser (`app.use(express.json())`)
    > 4. สร้าง Route `POST /dispatch`:
    >    - เมื่อมี Request เข้ามา ให้ `console.log('🚨 ALERT RECEIVED:', req.body)`
    >    - ส่ง Response กลับไปว่า `{ status: 'received' }`"
*   **✅ VERIFY:**
    1. รัน `node server.js`
    2. กลับไปหน้าเว็บ `localhost:5000` ลองกดแจ้งเหตุ (HIGH)
    3. ดู Terminal ของ **Agent Hub** — ต้องขึ้นข้อความ `🚨 ALERT RECEIVED: { ... }`

---

## 📱 PHASE 2: THE TANGIBLE AGENTS (เอเจนต์สัมผัสได้)
*เป้าหมาย: เปลี่ยน Data ให้เป็น Action ที่มนุษย์รับรู้ได้*

### Mission 11: The Pocket Agent (แจ้งเตือนเขย่าโลก)
การแจ้งเตือนที่ดีต้องเข้าถึงตัวมนุษย์ เราจะใช้ **ntfy.sh** (ระบบ Push Notification ฟรี ไม่ต้องลงแอปก็ใช้ได้)
*   **Concept:** **Third-party API Integration**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "สร้างโฟลเดอร์ `agents/` และไฟล์ `agents/ntfy-agent.js`:
    > 1. สร้างฟังก์ชัน `sendNotification(incidentData)`
    > 2. ให้ฟังก์ชันนี้ยิง POST request ไปที่ `https://ntfy.sh/overwatch_[YOUR_NAME]` (ตั้งชื่อ Channel ให้ไม่ซ้ำใคร)
    > 3. Body: `Title: 🚨 EMERGENCY ALERT!`, `Message: [TYPE] - [TEXT]`, `Priority: high`
    > 4. นำฟังก์ชันนี้ไป Import และเรียกใช้ใน `server.js` เมื่อได้รับ `/dispatch`"
*   **✅ VERIFY:** เปิดมือถือเข้าเว็บ `ntfy.sh/overwatch_[ชื่อคุณ]` รอไว้ แล้วลองแจ้งเหตุ HIGH — มือถือต้องเด้งเตือน!

### Mission 12: The Secretary Agent (เลขา AI ออกรายงาน)
ผู้บริหารต้องการ "เอกสาร" เสมอ Agent ตัวนี้จะพิมพ์ PDF ให้อัตโนมัติ
*   **Concept:** **Binary File Generation & File System (fs)**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "ติดตั้ง `npm install pdfkit`. สร้างไฟล์ `agents/pdf-agent.js`:
    > 1. สร้างฟังก์ชัน `generateReport(incidentData)`
    > 2. ใช้ `PDFDocument` สร้างไฟล์ใหม่ ตั้งชื่อไฟล์ด้วย Timestamp เช่น `report-123456789.pdf`
    > 3. เขียน Header ตัวใหญ่สีแดง 'CONFIDENTIAL REPORT'
    > 4. เขียนรายละเอียด Incident ลงไป
    > 5. บันทึกไฟล์ลงในโฟลเดอร์ `reports/` (สร้างโฟลเดอร์นี้ด้วยถ้ายังไม่มี)
    > 6. นำไปเรียกใช้ใน `server.js`"
*   **✅ VERIFY:** แจ้งเหตุจบ ไปดูในโฟลเดอร์ `reports` จะเห็นไฟล์ PDF โผล่มา!

### Mission 13: The Liaison Agent (ประสานงานผ่าน Discord)
ทีมต้องการพื้นที่คุยงาน Agent ตัวนี้จะโยนระเบิดลงห้องแชท
*   **Concept:** **Webhook Rich Embedding**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "สร้างไฟล์ `agents/discord-agent.js`:
    > 1. ไปขอ Webhook URL จาก Discord Channel ของคุณ (Edit Channel > Integrations > Webhooks)
    > 2. สร้างฟังก์ชัน `sendDiscordAlert(incidentData)`
    > 3. ยิง POST ไปที่ URL นั้น โดยจัด Payload แบบ **'Embeds'**:
    >    - Color: Decimal Code สีแดง (16711680)
    >    - Title: รับแจ้งเหตุ : [TYPE]
    >    - Description: [TEXT]
    >    - Footer: 'Overwatch AI System'
    > 4. นำไปเรียกใช้ใน `server.js`"

---

## 🌐 PHASE 3: THE FRONT-END UPGRADES (ยกระดับศูนย์บัญชาการ)
*(หมายเหตุ: กลับมาทำที่โปรเจกต์ overwatch-starter / หน้าเว็บ index.html)*

### Mission 14: The Danger Zone (รัศมีอันตราย)
บนหน้าจอ Dashboard เราต้องเห็นขอบเขตความเสียหาย
*   **Concept:** **Geospatial Visualization**
*   **👉 PROMPT ACTION (ทำที่ NestJS - หน้า HTML):**
    > "อัปเกรด `index.html`:
    > ในฟังก์ชันที่รับ Socket `new_incident`:
    > 1. ถ้าเหตุการณ์เป็น `HIGH` ให้สร้างวงกลม `L.circle` รอบพิกัด
    > 2. กำหนด Style: สีแดง (`color: 'red'`), โปร่งแสง (`fillOpacity: 0.3`), รัศมี 500 เมตร
    > 3. ให้วงกลมนี้กระพริบได้ หรือหายไปเองเมื่อผ่านไป 10 วินาที"

### Mission 16: The Adaptive HUD (ระบบเตือนภัยขั้นสุด)
หน้าจอต้องตอบสนองต่ออารมณ์ของเหตุการณ์
*   **Concept:** **Dynamic DOM Manipulation**
*   **👉 PROMPT ACTION (ทำที่ NestJS - หน้า HTML):**
    > "เขียน JavaScript เพิ่มเติม:
    > 1. สร้างตัวแปร Global `activeEmergencies` เพื่อนับจำนวนเหตุการณ์ที่ยังไม่ถูกจัดการ
    > 2. เมื่อได้รับ Socket Event: ถ้าเป็น HIGH ให้ `activeEmergencies++`
    > 3. ถ้า `activeEmergencies >= 3`:
    >    - เปลี่ยน Class ของ `<body>` เป็น `mode-defcon-1`
    >    - CSS ของ `mode-defcon-1` ให้เปลี่ยนพื้นหลังเป็นสีแดงเข้มกะพริบๆ และซ่อน Element ที่ไม่จำเป็น"

---

## ⚙️ PHASE 4: ADVANCED AUTOMATION & DEVOPS (สู่ความเป็น Enterprise)
*เป้าหมาย: ระบบที่ดูแลตัวเองได้ (Autonomous System)*

### Mission 17: The Postman Agent (จดหมายเหตุดิจิทัล)
อีเมลยังคงเป็นมาตรฐานราชการ เราจะส่งเมลโดยไม่ต้องมี Server จริง
*   **Concept:** **SMTP Protocol & Mocking**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "ติดตั้ง `npm install nodemailer`. สร้าง `agents/email-agent.js`:
    > 1. ใช้ `nodemailer.createTestAccount()` เพื่อสร้างบัญชี Ethereal (Fake SMTP)
    > 2. สร้าง Transporter จาก Account นั้น
    > 3. เขียนฟังก์ชัน `sendEmail(incident)` ส่งอีเมลจ่าหน้าถึง 'commander@overwatch.ai'
    > 4. **สำคัญ:** ให้ `console.log(nodemailer.getTestMessageUrl(info))` ออกมาด้วย เพื่อให้เรากดลิงก์ไปดูหน้าตาอีเมลได้จริง"

### Mission 19: The Timekeeper (ผู้คุมเวลา)
บางงานต้องทำเป็นรอบๆ ไม่ใช่ Real-time (เช่น สรุปยอด)
*   **Concept:** **Cron Jobs / Scheduled Tasks**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "ติดตั้ง `npm install node-cron`.
    > ใน `server.js` (หรือแยกไฟล์ `cron-jobs.js`):
    > 1. ใช้ `cron.schedule('*/2 * * * *', ...)` เพื่อตั้งเวลาทุก 2 นาที
    > 2. ใน Function นั้น ให้สั่ง `console.log('⏳ Running scheduled maintenance...')`
    > 3. (Optional) สั่งให้ `pdf-agent` สร้างรายงานสรุปว่า 'System Healthy' ทุก 2 นาที"
*   **✅ VERIFY:** รอ 2 นาที ดู Terminal ว่ามี Log ขึ้นหรือไม่

### Mission 20: The Watchdog (สุนัขเฝ้าบ้าน)
ระบบที่ดีต้องรู้ตัวว่าเพื่อนตายแล้ว
*   **Concept:** **Health Check & Self-Healing Monitoring**
*   **👉 PROMPT ACTION (ทำที่ Agent Hub):**
    > "สร้าง `agents/watchdog-agent.js`:
    > 1. เขียนฟังก์ชัน `checkSystemHealth()`
    > 2. ใช้ `axios.get('http://localhost:3000/status')` ยิงไปหา NestJS ทุกๆ 10 วินาที (`setInterval`)
    > 3. **Critical:** หุ้มด้วย `try-catch`
    >    - ถ้า `try` สำเร็จ: `console.log('✅ Core Online')`
    >    - ถ้า `catch` (Error): แสดงว่า NestJS ดับ! ให้เรียก `ntfy-agent.sendNotification()` ยิงแจ้งเตือนทันทีว่า '🚨 SYSTEM DOWN: CORE DISCONNECTED'"
*   **✅ VERIFY:**
    1. รันทั้ง 2 Server ปกติ
    2. **Kill** NestJS (Ctrl+C)
    3. นับ 1..2..3..10 วินาที... มือถือคุณต้องสั่นแจ้งเตือนว่าระบบล่ม!

---
---

## 📦 PHASE 5: THE CONTAINMENT (แพ็คร่างใส่กล่อง)
*เป้าหมาย: สร้าง Docker Image ให้พร้อมส่งออกไปรันที่ไหนก็ได้ในจักรวาล*

### Mission 21: The Blueprints (พิมพ์เขียวจักรกล)
สร้าง `Dockerfile` เพื่อบอกวิธีประกอบร่าง
*   **Concept:** **Containerization**
*   **👉 PROMPT ACTION (ทำที่ Root ของ NestJS - overwatch-starter):**
    > "สร้างไฟล์ `Dockerfile` (ไม่มีนามสกุล):
    > 1. ใช้ Base Image `node:18-alpine`
    > 2. Copy ไฟล์ `package*.json` และรัน `npm install`
    > 3. Copy โค้ดทั้งหมด (`.`) เข้าไป
    > 4. รันคำสั่ง Build (`npm run build`)
    > 5. Expose Port 3000
    > 6. คำสั่งรัน `CMD ["npm", "run", "start:prod"]`"
*   **👉 PROMPT ACTION (ทำที่ Root ของ Agent Hub - overwatch-agents):**
    > "เช่นกัน... สร้าง `Dockerfile` ในโฟลเดอร์ `overwatch-agents`:
    > 1. ใช้ Base Image `node:18-alpine`
    > 2. `npm install`
    > 3. Copy โค้ด
    > 4. Expose Port 4000
    > 5. `CMD ["node", "server.js"]`"

### Mission 22: The Shipyard (อู่ต่อยาน)
ใช้ **Docker Compose** เพื่อสั่งรันทุกอย่างพร้อมกันในคำสั่งเดียว
*   **Concept:** **Orchestration**
*   **👉 PROMPT ACTION (ทำที่ Root ของ NestJS):**
    > "สร้างไฟล์ `docker-compose.yml` ไว้ที่ Root ของ `overwatch-starter`:
    > 1. Define Service `core`:
    >    - context: `.`
    >    - ports: `3000:3000`
    >    - environment: `DATABASE_URL=file:../data/db.sqlite`
    > 2. Define Service `hub`:
    >    - context: `../overwatch-agents` (ระบุ path ย้อนออกไปหา folder พี่น้อง)
    >    - ports: `4000:4000`
    >    - environment: `CORE_URL=http://core:3000`"

### Mission 23: The Launch (ปล่อยยาน)
*   **Concept:** **Deployment**
*   **👉 PROMPT ACTION (Terminal):**
    > "รันคำสั่ง `docker-compose up --build`. รอจนกว่าจะเห็น Log ของทั้ง 2 Service วิ่งขึ้นมาพร้อมกัน แล้วทดสอบเข้า `localhost:5000` (Frontend) เพื่อดูว่าระบบยังทำงานปกติหรือไม่"

---
**จบภารกิจ Day 2 (Extended): ระบบของคุณตอนนี้เป็น "Containerized Microservices" เต็มรูปแบบ พร้อม Deploy ขึ้น Cloud แล้ว!**
