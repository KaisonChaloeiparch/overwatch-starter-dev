# 📅 DAY 1: THE GENESIS (การกำเนิด)

---

## 🌅 MISSION 1: THE FIRST BREATH
**เป้าหมาย:** สร้าง REST API พื้นฐานจากความว่างเปล่า

**👉 PROMPT ACTION:**
> "Ultron, เริ่มต้นระบบ. 
> 1. สร้าง REST API Resource ชื่อ `incidents` (ประกอบด้วย Module, Controller, Service, Entity)
> 2. สร้าง Endpoint `GET /status` ที่ Return JSON: `{ "system": "ULTRON", "status": "ONLINE", "timestamp": [Now] }`
> ปฏิบัติการ."

**✅ VERIFY:**
1. **The Pulse:** เปิด Browser ไปที่ `http://localhost:3000/status` ต้องเห็น JSON ตอบกลับมา
2. **The Blueprint:** ลองเข้า `http://localhost:3000/api` ต้องเห็นหน้า Swagger UI (ถ้ายังไม่เห็น ให้สั่ง Ultron: "ช่วยติดตั้ง Swagger และตั้งค่าที่ path /api ให้หน่อย")

---

## 🌤️ MISSION 2: THE INFRASTRUCTURE
**เป้าหมาย:** สร้างฐานข้อมูล SQLite และออกแบบตารางข้อมูล

**👉 PROMPT ACTION:**
> "Maker ต้องการสร้างโครงสร้างพื้นฐาน (Infrastructure). 
> 1. ติดตั้ง `sqlite3`, `typeorm`, `@nestjs/typeorm`.
> 2. ตั้งค่า `AppModule` ให้ใช้ SQLite (`database.sqlite`) โดยเปิด `autoLoadEntities` และ `synchronize`.
> 3. ปรับปรุง Entity `Incident`: เพิ่มฟิลด์ `id` (UUID), `text`, `type`, `priority`, `createdAt`.
> แสดงแผนการและโค้ดครับ Ultron."

**✅ VERIFY:**
1. **The Database:** สังเกตในแถบไฟล์ด้านซ้าย ต้องมีไฟล์ `database.sqlite` งอกขึ้นมา
2. **The Schema:** เปิดหน้า Swagger (`/api`) แล้วกดดูที่ **Schemas** ด้านล่างสุด จะต้องเห็นโครงสร้างของ `Incident` ที่เราเพิ่งสร้าง

---

## 🧠 MISSION 3: THE VIRTUAL BRAIN
**เป้าหมาย:** สร้างระบบวิเคราะห์ความรุนแรง (Intelligence Logic)

**👉 PROMPT ACTION:**
> "Ultron, สร้างฟังก์ชัน `analyzeThreat(text: string)` ใน `IncidentsService`.
> ให้เขียน Logic วิเคราะห์ Keyword ที่ชาญฉลาด (เช่น ถ้ามีคำว่า 'ไฟ', 'ระเบิด', 'อุบัติเหตุ' ให้เป็น HIGH) 
> และคืนค่าเป็น JSON: `{ type: string, priority: 'HIGH' | 'LOW' }`
> (หมายเหตุ: ใช้ Logic ภายในก่อน ยังไม่ต่อ API ภายนอก)"

**✅ VERIFY:**
1. **The Logic Check:** ตรวจสอบไฟล์ `incidents.service.ts` ว่ามีฟังก์ชัน `analyzeThreat` และมีรายการคำหลัก (Keywords) ที่เรากำหนดไว้หรือไม่

---

## 🔗 MISSION 4: THE CONNECTION
**เป้าหมาย:** เชื่อมต่อสมองเข้ากับระบบจัดเก็บ (Logic -> Database)

**👉 PROMPT ACTION:**
> "เชื่อมต่อสมองเข้ากับระบบจัดเก็บ. 
> 1. ใน `IncidentsController`, แก้ไข POST Request ให้รับแค่ `{ "text": string }`.
> 2. ใน `IncidentsService`, เมื่อรับ text มา ให้ส่งไปวิเคราะห์ที่ `analyzeThreat()` ก่อน.
> 3. นำผลลัพธ์ที่ได้ (Type, Priority) บันทึกลง Database ร่วมกับ Text เดิม.
> เริ่มการผ่าตัด."

**✅ VERIFY:**
1. **The Acid Test:** ไปที่ Swagger (`/api`) กด **POST /incidents** -> **Try it out**
2. **Input:** ใส่ Body เป็น `{ "text": "เกิดเหตุไฟไหม้ที่ตึกพารากอน" }` แล้วกด Execute
3. **The Result:** ตรวจสอบ Response ใน Swagger:
   - ต้องได้ `priority: "HIGH"` กลับมาอัตโนมัติ
   - ลองเปลี่ยน text เป็น "แมวติดบนต้นไม้" แล้วยิงใหม่ -> ต้องได้ `priority: "LOW"`
4. **The Final Confirmation:** ลองใช้ **GET /incidents** เพื่อดูว่าข้อมูลทั้งหมดถูกบันทึกลงฐานข้อมูลจริงหรือไม่

---
**จบภารกิจวันที่ 1: ระบบ Overwatch ของคุณเริ่มมีชีวิตแล้ว!**